import { a, renderList, taskList } from "./index.js";

console.log("From ApiCalls file", a);

export const functionChecking = () => {
  console.log("Checking functionality of the function from another module");
};
// console.log(renderList(taskList));
